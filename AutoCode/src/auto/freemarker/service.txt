package ${config.servicePackage};

import base.BaseService;
import ljs.lib.StringUtils;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Propagation;
import org.springframework.transaction.annotation.Transactional;
import ${config.pojoPackage}.${config.pojoName};

import java.util.HashMap;
import java.util.Map;
/**
 * ${table.info} service
 **/
@Service
public class ${config.serviceName} extends BaseService
{
    //添加${table.info}
    @Transactional(propagation = Propagation.REQUIRED)
    public Map<String, Object> add(${config.pojoName} ${table.name.field()}) throws Exception
    {
        ${table.name.field()}Mapper.insert(${table.name.field()});
        return success(ADD_OK);
    }

    //修改${table.info}
    @Transactional(propagation = Propagation.REQUIRED)
    public void update(${config.pojoName} ${table.name.field()}) throws Exception
    {
        if (${table.name.field()}.getId() == null)
            throw new Exception("${table.keyField.info}不能为空");
        ${table.name.field()}Mapper.update(${table.name.field()});
    }

    //查询${table.info}
    @Transactional(readOnly = true)
    public HashMap<String, Object> search(${config.pojoName} ${table.name.field()}) throws Exception
    {
        return search(${table.name.field()}, ${table.name.field()}Mapper);
    }
}
