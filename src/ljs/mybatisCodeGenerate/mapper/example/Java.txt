package *{mapperPackageName};

import org.apache.ibatis.annotations.Param;

import *{pojoClassName};
import java.util.List;
/**
 * 通过代码生成器生成于*{generateDate}
 * @author https://github.com/LiuJiangshan/LjsJavaLib
 */
public interface *{simpleMapperClassName}
{
    /**
     * 通过ID查找对象
     */
    *{simplePojoClassName} findById(@Param("*{primaryFieldName}") int id);

    /**
     * 获取与其相似的数据数量
     */
    int getExampleCount(@Param("pojo") *{simplePojoClassName} pojo);

    /**
     * 获取与其相似的数据
     */
    List<*{simplePojoClassName}> findByExample(@Param("pojo") *{simplePojoClassName} pojo,
                          @Param("start") int start,
                          @Param("length") int length);

    /**
     * 插入数据
     */
    void insert(@Param("pojo") *{simplePojoClassName} pojo);

    /**
     * 更新数据
     */
    void update(@Param("pojo") *{simplePojoClassName} pojo);

    /**
     * 删除数据
     */
    void delete(@Param("*{primaryFieldName}") int *{primaryFieldName});
}

